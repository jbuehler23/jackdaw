# Node Graph
- Scrolling locked around cursor in node graph node
- Wire up node graph data to operator API
- Node graph add new widget that shows potential links based on supported fields
- Node graph groups as well
- Extension based graph, can add any sort of data/node - linked via relationship within a domain
- Add code example for nodes

# UI / UX
- Show errors from logs in the UI, notification framework perhaps
- Add + to vertical list, and tooltip hover for Window there
- Multiple instance not supported for Timeline
- Fields on material browser that can be edited, like colors

# Rendering
- Add gizmos to a specific RenderLayer to avoid copying gizmos into user-added cameras
- Modify texel density to maintain constant UV scaling - 1028/m as default maybe?
