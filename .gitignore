# Rust builds
/target
# xtask is a detached workspace with its own build output
xtask/target
# test fixtures target and cargo lock files
tests/fixtures/**/target/
tests/fixtures/**/Cargo.lock
# where `cargo xtask bundle` stages a release bundle: several GB
/dist
# mdBook build output
/book/book
# Large media files
*.mp4
# jsn
.jsn/project.jsn
.jsn/catalog.bsn
.jsn/*.bak
# editor config / generated caches
.jackdaw/
# web explorer app
web/explorer/node_modules
