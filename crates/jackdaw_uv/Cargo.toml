[package]
name = "jackdaw_uv"
version.workspace = true
edition = "2024"
description = "Engine-agnostic UV projection math for the jackdaw editor"
license = "MIT OR Apache-2.0"
repository = "https://github.com/jbuehler23/jackdaw"

[dependencies]
glam.workspace = true
# Path dep with no default features so the bevy-gated `render` feature of
# jackdaw_geometry is never pulled in; this keeps jackdaw_uv bevy-free.
jackdaw_geometry = { version = "0.19.0", path = "../jackdaw_geometry", default-features = false }

[lints]
workspace = true
