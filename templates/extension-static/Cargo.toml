[package]
name = "{{project-name}}"
version = "0.1.0"
edition = "2024"
publish = false

# Static extensions are plain library crates. The editor links the
# extension via `ExtensionPlugin::with_extension::<MyExtension>()`.
[lib]
path = "src/lib.rs"

[dependencies]
bevy = "0.18"
jackdaw_api = "0.4"
