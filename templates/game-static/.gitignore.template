# cargo build output
/target

# IDE clutter
.vscode/
.idea/

# Note: `Cargo.lock` is INTENTIONALLY tracked. Binary crates (which
# this template produces — both `cargo play` and `cargo editor` are
# bins) commit `Cargo.lock` so contributors and CI build the same
# dependency versions.
